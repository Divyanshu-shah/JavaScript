let num = 10.75;

// console.log(Math.floor(num)); // floor rounds a number down to the nearest integer

// console.log(Math.ceil(num)); // ceil rounds a number up to the nearest integer

// console.log(Math.round(num)); // round rounds a number to the nearest integer

// console.log(num.toFixed(2)); // toFixed formats a number using fixed-point notation and returns a string

// console.log(isNaN(num)); // isNaN checks if a value is NaN (Not-a-Number) and returns true or false

// console.log(Math.random()); // random returns a random number between 0 (inclusive) and 1 (exclusive)

console.log(Math.max(5, 10, 15)); // max returns the largest of zero or more numbers

console.log(Math.min(5, 10, 15)); // min returns the smallest of zero or more numbers

console.log(Math.pow(2, 3)); // pow returns the base to the exponent power, that is, base^exponent



