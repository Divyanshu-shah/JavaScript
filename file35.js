// const names=["Alice", "Bob", "Charlie", "David", "Eve"];

const names = new Array("Alice", "Bob", "Charlie", "David", "Eve");

// names.push("Frank"); // push adds an element to the end of the array

// names.pop(); // pop removes the last element of the array

// console.log(names);

// names.unshift("Frank"); // unshift adds an element to the beginning of the array

// names.shift(); // shift removes the first element of the array

// console.log(names);

// console.log(names.sort()); // sort sorts the array in alphabetical order

// console.log(names.reverse()); // reverse reverses the order of the array

const numbers = [5, 2, 9, 1, 5, 2];

// console.log(numbers.sort((a, b) => a - b)); // sort numbers in ascending order

// console.log(numbers.sort((a, b) => b - a)); // sort numbers in descending order

// console.log(numbers.indexOf(2)); // indexOf returns the first index of the specified element

// console.log(numbers.lastIndexOf(2)); // lastIndexOf returns the last index of the specified element

// console.log(numbers.includes(3)); // includes returns true if the array contains the specified element, otherwise false

console.log(numbers.slice(2, 5)); 

